class Vectors():

    def q1():
        #Question: <1, 2, 3> + <2, 3, 4>.

        ans1 = input("answer: ")
        if ans1 == "<3,5,7>" or ans1 == "<3, 5, 7>":
            print("\nGreat! Now try it with different numbers.")
        else:
            print("\nNot quite. Remember to add each component separately. Rerun the cell to try again.")


    def q2():
        #Question: <-5, 2, 0> + <-15, -2, 0>.

        ans1 = input("answer: ")
        if ans1 == "<-20,0,0>" or ans1 == "<-20, 0, 0>":
            print("\nGreat! Now try it with different numbers.")
        else:
            print("\nNot quite. Remember to add each component separately. Rerun the cell to try again.")

    def q3():
        #Question: <0, 5, 6> + <15, 25, 76>.

        ans1 = input("answer: ")
        if ans1 == "<0.015, 5.025, 6.076> m" or ans1 == "<0.015,5.025,6.076> m":
            print("\nWonderful!")
        else:
            print("\nNot quite. Remember to convert the units. Rerun the cell to try again.")

    def q4():
        #Question: <0, 1, 4> + <6, 4, 4>.

        ans1 = input("answer: ")
        if ans1 == "<6, 5, 8>" or ans1 == "<6,5,8>":
            print("\nAmazing!")
        else:
            print("\nNot quite. Rerun the cell to try again.")

    def q5():
        #Question: <1, 2, 3> - <5, 6, 7>.

        ans1 = input("answer: ")
        if ans1 == "<-4, -4, -4>" or ans1 == "<-4,-4,-4>":
            print("\nAmazing!")
        else:
            print("\nNot quite. Rerun the cell to try again.")

    def q6():
        #Question: <3, 6, 9> - <3, 2, 1>.

        ans1 = input("answer: ")
        if ans1 == "<0, 4, 8>" or ans1 == "<0,4,8>":
            print("\nAmazing!")
        else:
            print("\nNot quite. Rerun the cell to try again.")

    def q7():
        #Question: <10, 16, 35> cm - <56, 14, 12> mm. Answer in meters.

        ans1 = input("answer: ")
        if ans1 == "<0.044, 0.146, 0.338> m" or ans1 == "<0.044,0.146,0.338> m":
            print("\nAmazing!")
        else:
            print("\nNot quite. Rerun the cell to try again.")

    def q8():
        #Question: <67, 15, 5> N/C - <5, 8, 2> kN/C. Answer in N/C.

        ans1 = input("answer: ")
        if ans1 == "<-4933, -7985, -1995> N/C" or ans1 == "<-4933,-7985,-1995> N/C":
            print("\nAmazing!")
        else:
            print("\nNot quite. Rerun the cell to try again.")

    def q9():
        #Question: What is q8 in V/m.

        ans1 = input("answer: ")
        if ans1 == "<-4933, -7985, -1995> V/m" or ans1 == "<-4933,-7985,-1995> V/m":
            print("\nAmazing!")
        else:
            print("\nNot quite. Rerun the cell to try again.")

    def q10():
        #Question: 5 * <6, 7, 8>.

        ans1 = input("answer: ")
        if ans1 == "<30, 35, 40> m/s" or ans1 == "<30,35,40> m/s":
            print("\nAmazing!")
        else:
            print("\nNot quite. Rerun the cell to try again.")

    def q11():
        #Question: 3 * <8, 2, 1>.

        ans1 = input("answer: ")
        if ans1 == "<24, 6, 3> m/s^2" or ans1 == "<24,6,3> m/s^2":
            print("\nAmazing!")
        else:
            print("\nNot quite. Rerun the cell to try again.")

    def q12():
        #Question: |<5, 2, 1>| N/C.

        ans1 = input("answer: ")
        if ans1 == "5.477 N/C":
            print("\nAmazing!")
        else:
            print("\nNot quite. Rerun the cell to try again.")

    def q13():
        #Question: |<0, 5, 5>|.

        ans1 = input("answer: ")
        if ans1 == "7.071":
            print("\nAmazing!")
        else:
            print("\nNot quite. Rerun the cell to try again.")

    def q14():
        #Question: |<3, 2, 4>|.

        ans1 = input("answer: ")
        if ans1 == "5.385 V/m":
            print("\nAmazing!")
        else:
            print("\nNot quite. Rerun the cell to try again.")

    def q15():
        #Question: rhat of <7, 6, 5>. 49, 36, 25 10.4881

        ans1 = input("answer: ")
        if ans1 == "<0.667, 0.572, 0.477>" or ans1 == "<0.667, 0.572, 0.477>":
            print("\nAmazing!")
        else:
            print("\nNot quite. Rerun the cell to try again.")

    def q16():
        #Question: -6 * <3, 4, 5>

        ans1 = input("answer: ")
        if ans1 == "<-18, -24, -30>" or ans1 == "<-18,-24,-30>":
            print("\nAmazing!")
        else:
            print("\nNot quite. Rerun the cell to try again.")

    def q17():
        #Question: <1, 6, -2> dot <2, -1, 3>

        ans1 = input("answer: ")
        if ans1 == "-10":
            print("\nAmazing!")
        else:
            print("\nNot quite. Rerun the cell to try again.")

    def q18():
        #Question: <2, 2, -1> dot <1, -6, 2>

        ans1 = input("answer: ")
        if ans1 == "-12":
            print("\nAmazing!")
        else:
            print("\nNot quite. Rerun the cell to try again.")

    def q19():
        #Question: 6 * 10 * cos(30)

        ans1 = input("answer: ")
        if ans1 == "51.962":
            print("\nAmazing!")
        else:
            print("\nNot quite. Rerun the cell to try again.")

    def q20():
        #Question: <2, 4, 6> cross <3, 2, 5>

        ans1 = input("answer: ")
        if ans1 == "<8, 8, -8>" or ans1 == "<8,8,-8>":
            print("\nAmazing!")
        else:
            print("\nNot quite. Rerun the cell to try again.")

    def q21():
        #Question: <-1, -5, 6> cross <4, -5, 9>

        ans1 = input("answer: ")
        if ans1 == "-15i + 33j + 25k":
            print("\nAmazing!")
        else:
            print("\nNot quite. Rerun the cell to try again.")

    def q22():
        #Question: <4, -5, 9> cross <-1, -5, 6>

        ans1 = input("answer: ")
        if ans1 == "15i - 33j - 25k":
            print("\nAmazing!")
        else:
            print("\nNot quite. Rerun the cell to try again.")
